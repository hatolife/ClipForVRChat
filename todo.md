# Current Todo

## v0.1.8 最小要件チェックリスト

- [x] 1. 必要な設定値を設定画面から編集できる
  - [x] `自動撮影` タブを追加する。
  - [x] 自動撮影ON/OFF、撮影間隔、初回待機、開始時撮影を設定できる。
  - [x] OSCホスト、送信ポート、受信ポート、現在Pose保存の有効秒数を設定できる。
  - [x] 撮影方式として実装済み扱いの `stream` / `photo` を選択できる。
  - [x] Stream方式の `ffmpeg` パス、入力引数、タイムアウトを設定できる。
  - [x] Stream切り出し保存先、ファイル名テンプレート、sidecar JSON設定を編集できる。
  - [x] Presence/output log監視、Discord投稿、同席ユーザー表示設定を編集できる。
  - [x] 構図ごとのON/OFF、Pose、拡大率、表示マスク、順序、複製、削除を編集できる。
  - [ ] 現在のUIに未実装または誤解を招く項目が残っていないか再確認する。

- [ ] 2. 設定値初期値に従ってゲーム内でカメラ機能が動作する
  - [x] 初期構図として `正面`、`背後`、`斜め` を持つ。
  - [x] 構図ごとに撮影する/しないのトグルを持つ。
  - [x] 構図ごとの拡大率を持つ。
  - [x] 初期Poseへ戻すボタンを持つ。
  - [x] 各構図のテスト撮影ボタンを持つ。
  - [x] 各構図の設定Poseへカメラ移動するボタンを持つ。
  - [x] `/usercamera/Mode`、`/usercamera/Pose`、Zoom、表示マスクなどをOSC送信する。
  - [x] カメラ操作が詰まった場合の `カメラOSCをリセット` ボタンを持つ。
  - [ ] 実機で「カメラを開けなくなる」状態が解消されたことを確認する。
  - [ ] 初期3構図が、ユーザー想定の正面/背後/斜めの構図になることを実機で確認する。
  - [ ] プレイヤー中心のローカル座標系として扱う仕様を確定する。現状はワールドPoseとLookAtMe/LocalPlayer設定であり、真のプレイヤーローカル座標変換ではない。

- [ ] 3. Stream Camera方式でシャッター音なしに撮影できる
  - [x] `ffmpeg` の存在確認ボタンを持つ。
  - [x] `winget install ffmpeg` を実行するインストールボタンを持つ。
  - [x] `ffmpeg` 未検出時に未インストール/PATH未設定として表示する。
  - [x] Stream方式で画像ファイルを出力し、sidecar JSON、Discord投稿、履歴連携へ流せる。
  - [x] 旧RCの `desktop` / `title=VRChat` 初期入力を新入力へ移行する。
  - [ ] VRChat Stream Camera/Spout出力から1フレームを取得する。本命要件として未完了。
  - [ ] デスクトップ全体、VRChatウィンドウ付近、白画像ではなく、Stream Camera映像そのものが保存されることを確認する。
  - [ ] 複数構図をできるだけ短時間で撮影できることを実機で確認する。
  - [ ] 失敗画像や誤キャプチャを成功扱いで履歴/Discordへ残さない検出を実装または方針決定する。

- [ ] 4. Photo方式をフォールバックとして動作させる
  - [x] Photo方式を選択できる。
  - [x] `/usercamera/Capture` を押下/解放OSCとして送る。
  - [x] 撮影後のVRChat写真ファイル検出を行う。
  - [x] 写真未検出時に詳細ログとエラーを出す。
  - [x] 失敗のみの結果を履歴に残さない。
  - [ ] カメラ未表示状態からPhoto方式で自動起動、撮影、終了できるかを実機で確認する。
  - [ ] Photo方式はシャッター音が出るため本命ではなく、フォールバックとしてUI/説明を最終確認する。

- [ ] 5. 撮影時に同じインスタンスにいたユーザー情報を取得する
  - [x] VRChat output logからjoin/leave相当のユーザー情報を解析する。
  - [x] 撮影バッチ開始時点のユーザー一覧をsnapshotする。
  - [x] sidecar JSONに同席ユーザー情報を保存する。
  - [x] Discord投稿本文に同席ユーザー名/IDを含める設定を持つ。
  - [ ] output log由来のため、アプリ起動前から同席していたユーザーを完全に復元できない制約をUI/仕様で明示する。
  - [ ] 現在インスタンスにいる全ユーザーが十分に取れているか、実機ログで確認する。

- [ ] 6. 取得されたユーザー情報を保持し、画像と紐づける
  - [x] 画像と同じ場所へsidecar JSONを保存する。
  - [x] sidecar JSONにbatch ID、shot ID、構図、撮影方式、画像SHA256、ユーザー一覧を含める。
  - [x] 履歴には成功した画像だけを追加する。
  - [x] Discord投稿結果を履歴へ紐づける。
  - [ ] ローカルDB相当の永続管理が必要か判断する。現状はsidecar JSONと既存history JSONで保持している。
  - [ ] 画像削除/履歴削除時にsidecar JSONとの整合性をどう扱うか確認する。

- [ ] 7. Discord投稿やExifなどに同席ユーザー情報を保持する
  - [x] Discord投稿本文に同席ユーザー情報を含められる。
  - [x] sidecar JSONに同席ユーザー情報を保持する。
  - [ ] EXIF書き込みは未実装。`writeExif` / `writeUserListToExif` 設定項目だけが存在する。
  - [ ] EXIFへユーザー情報を書き込むか、v0.1.8では明示的に断念してUIから外すか決める。
  - [ ] Discord投稿本文の長さ上限やユーザー数が多い場合の省略ルールを確認する。

- [ ] 8. 既存機能を壊さない
  - [x] Go testを通す。
  - [x] frontend buildを通す。
  - [x] GitHub Actions CIを通す。
  - [x] Release workflowを通す。
  - [ ] 通常画像処理、VRChat写真自動投稿、スクリーンショット自動投稿、履歴操作の既存動作を手動または追加テストで再確認する。
  - [ ] 自動撮影失敗時に既存履歴/Discord削除/ローカル削除機能へ悪影響がないことを確認する。

- [x] 9. 解像度設定の扱いを決める
  - [x] v0.1.8では撮影時の解像度一時変更は断念する。
  - [x] UI説明で、VRChat側の現在設定を使用することを明記する。
  - [x] 未完成の解像度変更UIを出さない。

- [ ] 10. GitFlowとRCリリース確認
  - [x] `master` へ直接入れず、`develop`、作業ブランチ、`release/v0.1.8` で作業する。
  - [x] v0.1.8 RCタグをGitHubへpushする。
  - [x] 最新RCのCI/Release成功を確認する。
  - [ ] 未完了項目を実装後、次RCタグを作成してCI/Release成功を確認する。
  - [ ] v0.1.8正式化前に `issues/README.md` の対象チケット状態を最終更新する。
