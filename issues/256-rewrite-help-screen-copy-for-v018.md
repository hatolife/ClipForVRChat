# v0.1.8仕様に合わせて使い方画面の文面草案を作る

## 指示

使い方画面の文章チェックしたい v0.1.8で仕様変更などあってるので 基本的な使い方　発展的な使い方 こんな機能もあるよ　段階を踏む感じにしたい 現在カード形式になってるが普通に１つの文章のようにしたい まず文面考えて草案をissueにかいて

追記:

256をこちらでちょっと編集した 機能概要部分までみて !の部分は指示と思って修正して

追記:

VRChat写真フォルダを設定しておくと、ゲーム内で撮影された新しい写真を検出して自動で処理できます。
  VRChat写真フォルダを設定する機能って設定にあったっけ

変更しました

追記:

issueに使い方画面の変更のやつがあったと思うのです 探して状況おしえて

## 文脈

現在の使い方画面は「画像を用意する」「必要なら設定する」「Discordへ投稿する」「結果を使う」の4カード構成になっている。v0.1.8では、VRChat写真/スクリーンショットの自動処理に加えて、自動撮影、AvatarBeacon、Stream Camera(Spout)、OSCログ、診断データ生成などの説明が必要になっている。

一方で、使い方画面にすべての設定項目を並べると初見で読みづらい。ユーザーが最初に必要な操作から順に読み進められるよう、「基本的な使い方」「発展的な使い方」「こんな機能もあるよ」の段階に分ける。

## 解釈

まずは実装せず、使い方画面に載せる文章の草案をissueに記録する。UIはカードを並べる形式ではなく、見出しと本文が続く読み物に近い構成を想定する。本文はv0.1.8時点の仕様に合わせ、Stream方式はSpout主経路、AvatarBeaconは自動撮影の基準取得用、Camera UIは手動起動が必要な場合がある、という前提で書く。

## 問題

- 現行の使い方画面がv0.1.8の主要機能を十分に説明していない。
- カード形式のため、基本操作から高度な使い方へ段階的に読み進めにくい。
- 自動撮影、AvatarBeacon、Stream Camera(Spout)などの関係が初見で分かりにくい。

## 期待する挙動

- 使い方画面が、基本操作から発展機能へ自然に読み進められる文面になる。
- v0.1.8の仕様変更と現在の制約が誤解なく伝わる。
- カード形式ではなく、1つの文章として読める構成になる。

## 受け入れ条件

- [ ] 使い方画面用の文面草案がissueに記録されている。
- [ ] 草案が「基本的な使い方」「発展的な使い方」「こんな機能もあるよ」の段階を含む。
- [ ] 自動撮影、AvatarBeacon、Stream Camera(Spout)、Discord投稿、ローカル保存、不具合報告用データの説明がv0.1.8仕様に合っている。
- [ ] 実装前に文面レビューできる状態になっている。

## 文面草案

### 使い方

ClipForVRChatは、VRChatで使う画像URLを作ったり、撮影した画像を保存・投稿したりするためのアプリです。

### 機能概要

1. 画像を処理する
   - 既定では、大きい画像を最大2048x2048に収まるよう縮小できます。
   - 画像ファイルをドラッグ&ドロップして処理できます。
   - クリップボードに入っている画像も処理できます。
   - 複数画像をまとめて処理できます。
   - VRChat写真フォルダを監視して、ゲーム内で撮影された新しい写真を自動で処理できます。
   - スクリーンショットフォルダを監視して、Windows+Shift+Sなどで保存された画像を自動で処理できます。

2. 処理した画像を保存・投稿する
   - ローカルフォルダへ保存できます。
   - Discord Webhookへ投稿できます。
     - 投稿後の画像URLをコピーできます。
   - QRコードURL検出をONにすると、画像内のQRコードURLを結果画面やDiscord本文に含められます。

3. VRChatのカメラで自動撮影する
   - AvatarBeaconを使うと、アバター基準の構図を設定し、自動でカメラを移動させて撮影できます。
     - OSCでUser Cameraを操作して、設定した構図や間隔で撮影できます。
     - Stream Camera(Spout)の映像を取得して画像として保存できます。
   - フォールバックモードでは、VRChat内であらかじめ配置したローカルアンカーCameraを使って撮影できます。
     - AvatarBeaconを使えない場合でも撮影できます。
     - ClipForVRChatはカメラ移動を送信しません。
     - 構図ごとの位置、拡大率、表示対象などは使われず、VRChat内で配置済みのカメラ状態をそのまま使います。

4. 結果やログを確認する
   - 履歴画面で過去の処理結果を確認・整理できます。
     - 履歴画面からDiscordに投稿した画像を削除できます。
   - OSCタブで送受信OSCを確認できます。
   - 不具合報告用データを生成し、設定、履歴、ログをまとめて送れる形にできます。

### 1. 画像縮小

まずは、処理したい画像ファイルをこのウィンドウへドラッグ&ドロップします。
クリップボードに入っている画像を使う場合は、「クリップボード画像を処理」ボタンから処理できます。
複数の画像をまとめて処理することもできます。

既定では、大きい画像は最大2048x2048に収まるよう縮小されます。それ以下の場合縮小しません。
画像形式、品質、ファイル名のサフィックスなどは設定画面から変更できます。


### 2. 画像保存・投稿

処理した画像は、保存、Discord投稿、URLコピーができます。
保存だけ投稿はしない、といった使い方も可能です。

VRChatで撮った写真を毎度貼り付けるのが面倒な場合は、自動化できます。
設定 > 機能 > VRChat写真自動処理 をONにしてください。

Windows+Shift+Sなどで保存したスクリーンショットも自動で処理できます。
これには 設定 > 機能 > スクリーンショット自動処理 をONにします。

QRコード読み取り機能を使いたい場合は、設定 > 機能 > QRコードURL検出 をONにします。
QRコードは１枚の画像に複数個合ってもある程度対応していますが、完全ではないので注意してください。

処理結果は結果画面に表示されます。画像URLがある結果ではURLをコピーできます。
ローカル保存した結果では、保存先を開けます。
1枚だけ処理した場合は、設定がONなら画像URLを自動でクリップボードへコピーします。

Discordへ投稿することもできます。
設定 > Discord投稿 から投稿先チャンネルのWebhook URLを登録してください。
Webhook URLの作り方は、Discord公式ヘルプを確認してください。
https://support.discord.com/hc/ja/articles/228383668-%E3%82%A6%E3%82%A7%E3%83%96%E3%83%95%E3%83%83%E3%82%AF%E3%81%AE%E3%81%94%E7%B4%B9%E4%BB%8B
Webhook URLは機能ごとに分けて設定可能です。


### 3. 自動撮影

VRChat内のカメラを使って定期的に撮影したい場合は、自動撮影を使います。
自動撮影は、VRChatのカメラを自動で定期的に撮影する機能です。

2つモードがあり初期状態ではフォールバックモードで動作します。

フォールバックモードでは、VRChat内であらかじめ配置したカメラを使って撮影します。
ClipForVRChatは端に自動で定期的に撮影だけします。
アンカーをローカルに設定しておくとアバターが移動しても追従してくれるので便利かと思います。

フォールバックモードがOFFの時、通常モードで動作します。
通常モードでは事前に決めた構図にカメラを自動で移動させて撮影できます。
構図は何個でも設定可能です。
初期値では一定時間ごとに、正面、背面、斜めからの構図で3枚撮影します。

通常モードを使用するためには、アバターギミックの導入が必須となります。
	なぜアバターギミックの導入が必要なのか解説します。

	OSCでカメの位置を制御するにはカメラを配置する座標をワールド座標系で指定する必要があります。
	ワールド座標系でアバター基準でカメラを配置するにはアバターの位置情報が必要です。
	例えば、アバターの正面1mくらいのところにカメラ配置したい、というのを実現するには、
	アバターがワールド内のどこにいるのか調べ、アバターの向きを調べ、
	アバターの正面1mの位置を計算する必要があります。
	しかし、公式の方法ではワールド内のアバター位置を取得する方法が提供されていません。
	ワールド側のギミックとしてなら可能ですが、その場合特定のワールドだけでしか使えない機能になってしまいます。
	よって、任意のワールドでアバター基準にカメラを自動配置することは、公式機能だけでは難しいです。

	ClipForVRChatではこの問題を解決するため、
	ワールド内のプレイヤーの位置情報をOSCで送信するアバターギミック AvatarBeacon を作成しました。
	AvatarBeaconは、プレーヤーのHeadの座標と向きをOSCで送信するアバターギミックです。

	AvatarBeaconから送信されたプレーヤーの位置と向き情報を使って、
	ClipForVRChatで配置先カメラ位置を計算し、アバター基準の自動撮影を実現しています。


	AvatarBeaconをアバターへ導入する方法を解説します。
	!ここに記載

また、フォールバックモードでも通常モードでも、ゲーム内でカメラを手動で起動しておく必要があります。
これは、2026年7月時点のVRChatでは、Camera UIを閉じるとOSCによるカメラ制御が効かない不具合があるためです。
そのため、自動撮影を使うときはゲーム内でカメラを手動で起動しておく必要があります。


### 4. 結果・ログ確認

履歴画面では、これまで処理した画像の結果を確認できます。
Discordへ投稿した画像、ローカル保存した画像、履歴だけに残っている画像を区別しながら、
必要に応じて削除や整理ができます。
履歴画面からDiscordに投稿した画像を削除することもできます。
削除しないようピン留めも可能です。

OSCタブでは、VRChatから受信したOSCやClipForVRChatから送信したOSCを確認できます。
他のOSCアプリも同時に使いたい場合は、VRChatから届いたOSCを別ポートへ転送できます。

不具合報告が必要なときは、情報画面から不具合報告用データを生成できます。

## 実装メモ

- 現行の `help-grid` / `help-card` 形式をやめ、本文中心の構成に変更する。
- Discord公式Webhookヘルプへのボタンは、画像保存・投稿のDiscord説明付近へ残す。
- 自動撮影の詳細説明は設定画面にもあるため、使い方画面では導入順と注意点に絞る。
- OSCログファイル名は現仕様どおり `osc_recieve.jsonl` の綴りに合わせる。

## 調査メモ

- 2026-07-07: 使い方画面変更の現行チケットはこのissue。`issues/README.md` では状態 `要対応`、対応バージョン `未定`。
- 2026-07-07: `src/frontend/src/main.js` の使い方画面はまだ `help-grid` / `help-card` の4カード構成で、本文中心の構成への実装反映は未完了。
- 2026-07-07: 草案作成は進んでいるが、受け入れ条件のチェックは未完了のまま。
