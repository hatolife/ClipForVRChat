# 開発用アイコン生成ツールの gosec 指摘対応

## 問題

`src/tools/make_icon.go` に `gosec` の低優先度指摘が残っている。配布アプリ本体への影響は低いが、セキュリティチェックを CI に入れる場合に失敗要因になる。

## 期待する挙動

開発用ツールの権限・整数変換の指摘を解消し、`gosec` のノイズを減らす。

## 受け入れ条件

- アイコン生成先ディレクトリの権限が絞られる。
- `uint32 -> uint8` 変換の安全性がコード上明確になる。
- `os.Create` の変数パス指摘が実害のない形に整理される。
