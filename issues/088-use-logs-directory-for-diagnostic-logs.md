# 088 診断ログ実フォルダ名をlogsへ統一する

## 問題

アプリ実フォルダでは診断ログが `log/` に出力され、診断zip内では `logs/` に格納されている。
同じログを指すフォルダ名が揺れている。

## 期待する挙動

- アプリ実フォルダの診断ログ出力先も `logs/` に統一する。
- 診断zip内も引き続き `logs/` を使う。

## 受け入れ条件

- 診断ログが `logs/YYYY-MM-DD.log` に出力される。
- 診断zip内のログも `logs/*.log` に格納される。
- 既存テストが `logs/` 前提に更新される。
