# v0.1.8最小要件のtodoを再整理する

## 問題

直近の `todo.md` がRCごとの修正完了リストになっており、v0.1.8として最低限実装しなければならない項目と、現時点で完了/未完了の認識が分かりにくい。
特にStream方式は、現在の実装がユーザーの想定するVRChat Stream Camera/Spout取得に到達していない。

## 期待する挙動

`todo.md` にv0.1.8の最小要件を大項目と小項目に分けて記載し、実装済みと未完了を明確にする。

## 受け入れ条件

- `todo.md` にv0.1.8の大項目チェックリストがある。
- 大項目ごとにインデントされた小項目がある。
- 実装完了と認識している項目は完了扱いにする。
- 不完全または実機未確認の項目は未完了扱いにする。
- Stream方式の未完了範囲を明示する。
