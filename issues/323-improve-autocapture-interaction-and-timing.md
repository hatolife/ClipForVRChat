# 自動撮影中の設定保持・終了処理・定期撮影UI・待機時間調整を改善する

## 指示

> ok
>
> 自動撮影終了時に設定を開いていると自動で閉じられてしまい変更が破棄される
> 設定を開いているときは撮影結果画面を表示しないようにしたい
>
> 自動撮影終了時にカメラを対比するのではなく、飛行モードをOFFにするのをデフォルトにしたい
>
> **自動撮影スケジュール　**一定間隔でVRChatカメラ撮影を実行します。
> が自動撮影のON/FFというのはわかりずらい
> 機能タブに移動し、自動定期撮影にラベルを変更
>
> 自動撮影を高速化したい　各操作の待機秒数ms単位で変更できるようにして最適な待期期間を探りたい

## 文脈

`v0.1.8-rc5` では、自動撮影結果イベントを受信すると画面状態を常に結果画面へ切り替えるため、設定編集中でも設定画面が閉じる。また、自動撮影のON/OFFが自動撮影タブ内の「自動撮影スケジュール」として表示され、機能全体の有効化操作だと分かりにくい。撮影処理には固定値または未公開設定の待機時間があり、実機で短縮可能な値を比較しにくい。

## 解釈

- 設定画面を開いている間は、自動撮影結果と履歴を更新して通知は出すが、結果画面へ遷移しない。
- 自動撮影バッチ終了時は、任意の待機カメラ位置移動とは独立して `/usercamera/Flying=false` を最後に送る。
- 自動撮影の主スイッチを機能タブへ移し、表示名を「自動定期撮影」にする。
- 撮影の主要待機時間をミリ秒単位で編集可能にし、0msを含む短い値を試せるようにする。異常時の検出timeoutは待機時間とは分けて維持する。

## 問題

- `auto-photo:result` の受信処理が現在の画面を考慮せず `mode: 'results'` を設定する。
- バッチ終了時のFlying OFFが最終操作として保証されていない。
- 定期撮影の有効化スイッチが機能タブにない。
- 構図安定待ち、ボタンrelease、Stream復旧、Camera Mode復元などの待機時間をGUIから比較できない。

## 期待する挙動

- 設定編集中の自動撮影完了では、未保存設定と表示中の設定画面を保持する。
- 設定画面以外では従来どおり撮影結果画面を表示する。
- 自動撮影終了時にFlyingをOFFへ送信する。
- 機能タブの「自動定期撮影」で自動撮影のON/OFFと間隔を設定できる。
- 主要な待機時間をms単位で変更し、診断ログで実際に使った値を確認できる。

## 受け入れ条件

- [ ] 設定画面で自動撮影結果を受信しても設定画面と未保存変更を維持する。
- [ ] 通常画面で自動撮影結果を受信した場合は結果画面へ遷移する。
- [ ] バッチ終了時の最後にFlying OFFを送信する。
- [ ] 自動定期撮影の主スイッチと設定を機能タブへ移動する。
- [ ] 撮影・Stream復旧・復元の主要待機時間をms単位で設定できる。
- [ ] 設定の既定値・正規化・処理適用を自動テストする。
- [ ] Frontend test、template検査、Wails API surface検査、Go test、Windows CIが成功する。

