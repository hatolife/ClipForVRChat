# Discord削除の部分成功を履歴へ保存する

## 問題

複数履歴をDiscordから削除する際、途中で失敗すると、それ以前に削除成功した履歴の `discordDeleted` 状態も保存されない。

## 期待する挙動

複数削除の一部が失敗しても、成功済みの削除状態は履歴に保存され、失敗分はユーザーへ分かる形で返る。

## 受け入れ条件

- 削除成功した履歴は途中失敗があっても `discordDeleted` と `deletedAt` が保存される。
- 失敗した履歴がある場合、UIでエラー内容を確認できる。
- 既存のピン止め除外挙動は維持される。
