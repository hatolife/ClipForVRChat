# 063 v0.1.6履歴のコミットメッセージと署名を整理する

## 状態

完了

## 問題

`169d7462f0d3231f0c6fdbf07a44e7d7d92655c3` より後のコミットメッセージが Angular 形式に揃っておらず、未署名コミットも含まれている。
また、試行錯誤の `Build Windows exe with console subsystem` とその revert が履歴に残っている。

## 期待する挙動

v0.1.6向けの履歴が Angular 形式のコミットメッセージに揃い、残すコミットはPGP署名済みになる。
相殺される試行錯誤コミットは履歴から整理されている。

## 受け入れ条件

- 対象範囲のコミットメッセージが `type(scope): subject` 形式になっている。
- 対象範囲の残すコミットがPGP署名済みになっている。
- console subsystem 試行コミットとその revert が履歴から除かれている。
- 履歴整理により変わった報告書内の対象コミット表記が更新されている。
- `v0.1.6` ローカルタグが整理後のHEADを指している。
