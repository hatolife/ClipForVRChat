# 033 WSLからのWindowsビルドスクリプト

## 状態

完了

## 背景

開発中にWSL環境からWindows向けexeをローカルビルドしたい。

## 対応内容

- WSLから `windows/amd64` 向けに `wails build` を実行する開発者用スクリプトを追加する。
- アプリのバージョン文字列には現在のコミットハッシュを使用する。
- 必要に応じてローカル確認用zipとsha256ファイルも生成できるようにする。
