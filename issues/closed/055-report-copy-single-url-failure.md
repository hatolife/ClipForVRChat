# URL自動コピー失敗をユーザーへ表示する

## 問題

1枚処理時のURL自動コピーが失敗してもエラーが握りつぶされ、成功メッセージが表示される可能性がある。

## 期待する挙動

URL取得に成功していてもクリップボードへのコピーに失敗した場合は、ユーザーが再コピーできるように明示される。

## 受け入れ条件

- URL自動コピー失敗時に成功メッセージだけが表示されない。
- 画像URL自体は結果画面からコピーできる。
- CLI/GUI双方の処理経路でコピー失敗を扱える。
