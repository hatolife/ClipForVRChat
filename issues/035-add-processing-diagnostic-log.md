# 035 処理診断ログの追加

## 状態

完了

## 背景

QRコード検出が期待通り動作しないケースの原因を調査できるよう、処理時の内部状態をログに残したい。

## 対応内容

- `config.json` と同じフォルダに `diagnostic.log` を追記する。
- 画像処理時に入力名、画像サイズ、出力サイズ、QR検出ON/OFF、QR検出結果を記録する。
- QR検出ライブラリが返したエラーも調査用に記録する。

## 注意

QRコード内のURLもログに残るため、ログファイルは調査用として扱う。
